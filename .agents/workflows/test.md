---
description: 运行后端单元测试
---

// turbo-all

## 运行测试

1. 运行所有测试：

```bash
cd d:\xueqiu_crx\backend && npm run test
```

2. 监听模式（开发时使用）：

```bash
cd d:\xueqiu_crx\backend && npm run test:watch
```

3. 生成覆盖率报告：

```bash
cd d:\xueqiu_crx\backend && npm run test:coverage
```
